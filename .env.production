# PostHog analytics — production configuration.
# These are NEXT_PUBLIC_ variables, so they are embedded in the client bundle
# and shipped to the browser. The PostHog project token is a public,
# write-only ingestion key and is safe to commit.
# Next.js loads this file automatically during `next build` in production.
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=phc_qVpqEPGmGKuKMhBx9uiPmEiMGdnhfameZXhjkZEvUems
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
